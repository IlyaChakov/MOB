# =========================
# ИСХОДНЫЕ ДАННЫЕ
# Заполняйте все данные, которые у вас есть.
# Если каких-то данных нет, оставьте NULL.
# =========================

sectors <- c("Сельское хозяйство", "Промышленность", "Услуги")

x <- c(120, 200, 180)

A <- matrix(
  c(
    0.10, 0.20, 0.05,
    0.15, 0.10, 0.10,
    0.05, 0.15, 0.10
  ),
  nrow = 3,
  byrow = TRUE
)

Z <- matrix(
  c(
    12, 40, 9,
    18, 20, 18,
    6, 30, 18
  ),
  nrow = 3,
  byrow = TRUE
)

y <- c(59, 144, 126)

# =========================
# НАСТРОЙКИ ЗАПУСКА
# =========================

ACTIVE_CASE <- "x_A"
ROUND_DIGITS <- 2
PRINT_TABLE <- TRUE
SAVE_TABLE_TO_CSV <- TRUE
OUTPUT_CSV <- file.path(script_dir, "balance_table.csv")
